import React from 'react';
import ReactDOM from 'react-dom/client';
import '../../css/ProductCardBig.css'

// const ProductCardList = () => {
//   return (
//     <section className='productCardList'>

//     </section>
//   )
// }

const ProductCard = () => {
  return (
    <article className='productCard'>

    </article>
  )
}

const root = ReactDOM.createRoot(document.getElementById('root'));

// root.render(<ProductCard/>);

//variables

//image, title, seller


//price, stars, add to card button

//use the images in the public folder first. we will change to src folder later